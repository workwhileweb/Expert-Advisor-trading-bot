[project]
name = "expert-advisor-mt5-tools"
version = "0.1.0"
description = "Scripts for MT5 data via MetaTrader5 Python API"
requires-python = ">=3.10"
dependencies = [
    "MetaTrader5>=5.0.45",
    "numpy>=1.24",
]

[tool.uv]
package = false
